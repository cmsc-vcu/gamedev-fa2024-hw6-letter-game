using UnityEngine;

public class PositionCircleScript : MonoBehaviour
{
    public GameObject Owner;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
